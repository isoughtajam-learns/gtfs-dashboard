# Used by `vite dev` (mode=development) - covers both local dev and the
# Docker dev container, since both run `npm run dev`.
#
# VITE_POSTHOG_KEY intentionally left unset: PostHog is disabled in dev (see
# src/main.tsx) so local/test traffic doesn't pollute production analytics.
