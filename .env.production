# Used by `vite build` (mode=production) - baked into the static bundle
# shipped in the nginx image. PostHog project API keys are meant to be
# public/client-embedded (unlike a server secret), so this is safe to commit.
VITE_POSTHOG_KEY=phc_kVkAeCRVJFNZ3XHfwNTtYzcih2wZ3RLkAS6wz2PtXsBs
VITE_POSTHOG_API_HOST=https://us.i.posthog.com
